CREATE TABLE "public"."invite_user" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "createdAt" timestamptz NOT NULL DEFAULT now(), "type" text NOT NULL, "inviteCode" uuid NOT NULL DEFAULT gen_random_uuid(), "expiryAt" timestamptz NOT NULL, "maxUseCount" integer NOT NULL DEFAULT 1, PRIMARY KEY ("id") , FOREIGN KEY ("type") REFERENCES "public"."user_type"("name") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"), UNIQUE ("inviteCode"));COMMENT ON TABLE "public"."invite_user" IS E'Contains secret invite code for patients & staff sign-ups';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
