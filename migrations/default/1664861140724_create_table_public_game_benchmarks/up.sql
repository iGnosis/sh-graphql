CREATE TABLE "public"."game_benchmarks" ("gameId" uuid NOT NULL, "createdAt" timestamptz NOT NULL DEFAULT now(), "analytics" jsonb NOT NULL DEFAULT jsonb_build_array(), PRIMARY KEY ("gameId") , FOREIGN KEY ("gameId") REFERENCES "public"."game"("id") ON UPDATE restrict ON DELETE restrict);COMMENT ON TABLE "public"."game_benchmarks" IS E'Stores analytics generated by a benchmark test';
