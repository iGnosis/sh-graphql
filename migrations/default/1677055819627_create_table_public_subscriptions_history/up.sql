CREATE TABLE "public"."subscriptions_history" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "createdAt" timestamptz NOT NULL DEFAULT now(), "subscriptionPlanId" uuid NOT NULL, "subscriptionId" uuid NOT NULL, "status" text NOT NULL, "startDate" timestamptz NOT NULL, "endDate" timestamptz NOT NULL, "patient" uuid NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("patient") REFERENCES "public"."patient"("id") ON UPDATE restrict ON DELETE restrict);COMMENT ON TABLE "public"."subscriptions_history" IS E'Keeps a track of patient\'s subscriptions.';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
