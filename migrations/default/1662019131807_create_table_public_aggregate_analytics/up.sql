CREATE TABLE "public"."aggregate_analytics" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "createdAt" timestamptz NOT NULL DEFAULT now(), "patient" uuid NOT NULL, "game" uuid NOT NULL, "key" text NOT NULL, "value" numeric NOT NULL, "noOfSamples" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("patient") REFERENCES "public"."patient"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("game") REFERENCES "public"."game"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));COMMENT ON TABLE "public"."aggregate_analytics" IS E'A table to store analytics which have been aggregated over a game.';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
