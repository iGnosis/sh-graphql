table:
  schema: public
  name: game_name
is_enum: true
select_permissions:
  - role: patient
    permission:
      columns:
        - name
      filter: {}
  - role: player
    permission:
      columns:
        - name
      filter: {}
