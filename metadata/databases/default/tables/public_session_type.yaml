table:
  name: session_type
  schema: public
is_enum: true
array_relationships:
  - name: sessions
    using:
      foreign_key_constraint_on:
        column: status
        table:
          name: session
          schema: public
