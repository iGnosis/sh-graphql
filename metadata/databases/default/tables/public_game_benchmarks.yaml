table:
  name: game_benchmarks
  schema: public
insert_permissions:
  - role: benchmark
    permission:
      check: {}
      columns:
        - analytics
        - gameId
        - originalGameId
        - systemSpec
select_permissions:
  - role: benchmark
    permission:
      columns:
        - analytics
        - createdAt
        - gameId
        - id
        - originalGameId
        - systemSpec
      filter: {}
update_permissions:
  - role: benchmark
    permission:
      columns:
        - analytics
        - originalGameId
        - systemSpec
      filter: {}
      check: null
