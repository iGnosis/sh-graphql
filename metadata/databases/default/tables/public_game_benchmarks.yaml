table:
  name: game_benchmarks
  schema: public
insert_permissions:
  - role: benchmark
    permission:
      check: {}
      columns:
        - analytics
        - gameId
        - systemSpec
select_permissions:
  - role: benchmark
    permission:
      columns:
        - analytics
        - systemSpec
        - createdAt
        - gameId
        - id
      filter: {}
update_permissions:
  - role: benchmark
    permission:
      columns:
        - analytics
        - systemSpec
      filter: {}
      check: null
