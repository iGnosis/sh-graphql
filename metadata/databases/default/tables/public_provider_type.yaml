table:
  name: provider_type
  schema: public
is_enum: true
array_relationships:
  - name: providers
    using:
      foreign_key_constraint_on:
        column: type
        table:
          name: provider
          schema: public
