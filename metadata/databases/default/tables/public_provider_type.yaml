table:
  schema: public
  name: provider_type
is_enum: true
array_relationships:
  - name: providers
    using:
      foreign_key_constraint_on:
        column: type
        table:
          schema: public
          name: provider
